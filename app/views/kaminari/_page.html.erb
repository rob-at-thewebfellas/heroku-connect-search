<%= content_tag(
      :span,
      link_to_unless(page.current?, page, url, rel: page.next? ? "next" : page.prev? ? "prev" : nil),
      class: [ page.current? ? "current" : nil, "page" ].compact.join(" ")
    ) %>